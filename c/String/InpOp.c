#include<stdio.h>
#include<string.h>
int main()
{
    // char str[40];
    // gets(str);
    // puts(str);
     
     char str[40];
     scanf("%s",str);  //here when taking ip using scanf only first word is considered
     printf("%s",str);
}
package packagA.greeting;
import static PackagB.mesage.message;
public class greeting {
    public static void main(String[] args) {
        System.out.println("here it is printing differenet");
        // see by creating two different packages we can have same class name
        //packageA is a folder(package)then greeting is sub folder(sub package)
        // in which there is a greeting file(class)
    }
} 

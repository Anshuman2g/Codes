#include<stdio.h>
#include<math.h>
#include<stdbool.h>
int main()
{
    // int n;
    // scanf("%d",&n);
    // int root= sqrt(n);
    // printf("%d",root);       

    //POWER
    int n,m;
    scanf("%d %d",&n,&m);
    int power=pow(n,m);
    printf("%d",power);
}